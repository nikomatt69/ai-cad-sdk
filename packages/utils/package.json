{
  "name": "@ai-cad-sdk/utils",
  "version": "2.2.0",
  "description": "Utility functions for AI CAD SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "vitest run",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@ai-cad-sdk/types": "2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "rimraf": "^5.0.0",
    "vitest": "^0.31.0"
  },
  "publishConfig": {
    "access": "public"
  }
}